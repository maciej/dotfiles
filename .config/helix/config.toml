theme = "catppuccin_macchiato"

[editor.soft-wrap]
enable = true

[keys.normal]
# Print the current line's git blame information to the statusline.
A-S-b = ":echo %sh{git blame -L %{cursor_line},+1 %{buffer_name}}"

[keys.insert]
C-space = "completion"
